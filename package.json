{
  "name": "Tensai Studio",
  "version": "1.0.0",
  "description": "Tensai Studio",
  "scripts": {
    "start": "dcl start -b -d -w",
    "build": "build-ecs",
    "watch": "build-ecs --watch",
    "deploy:now": "dcl export && now export",
    "ecs:install": "npm install -g --save-dev decentraland-ecs@latest",
    "ecs:install-next": "npm install -g  --save-dev decentraland-ecs@next"
  },
  "devDependencies": {
    "decentraland-ecs": "^6.6.6"
  },
  "dependencies": {
    "@dcl/ui-scene-utils": "^1.8.1",
    "@types/cannon": "^0.1.4",
    "cannon": "github:schteppe/cannon.js",
    "colyseus.js": "^0.14.12"
  },
  "bundleDependencies": [
    "@dcl/ui-scene-utils",
    "@dcl/ecs-scene-utils",
    "colyseus.js"
  ]
}
